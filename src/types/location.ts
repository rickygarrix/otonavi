// src/types/location.ts

export type Prefecture = {
  id: string;
  key: string;
  name: string;
  sort_order: number;
};

export type City = {
  id: string;
  key: string;
  name: string;
  sort_order: number;
};