{
  "$schema": "node_modules/wrangler/config-schema.json",

  "name": "otonavi-r2-worker",
  "main": "src/index.ts",
  "compatibility_date": "2026-02-05",

  // 🔥 今回は node 互換いらない（R2ネイティブのため）
  // 必要になったら戻せばOK
  // "compatibility_flags": ["nodejs_compat"],

  "observability": {
    "enabled": true
  },

  /**
   * ========================================
   * R2 Buckets（最重要）
   * ========================================
   */
  "r2_buckets": [
    {
      "binding": "R2_BUCKET",
      "bucket_name": "staging"
    }
  ]
}