{
  "$schema": "node_modules/wrangler/config-schema.json",

  "name": "otonavi-r2-worker",
  "main": "src/index.ts",
  "compatibility_date": "2026-02-05",

  "observability": {
    "enabled": true
  },

  /**
   * ===============================
   * 🔧 デフォルト（= staging）
   * ===============================
   * wrangler deploy のとき
   * → staging バケットに接続
   */
  "r2_buckets": [
    {
      "binding": "R2_BUCKET",
      "bucket_name": "staging"
    }
  ],

  /**
   * ===============================
   * 🚀 本番環境
   * ===============================
   * wrangler deploy --env production
   * → production バケットに接続
   */
  "env": {
    "production": {
      "r2_buckets": [
        {
          "binding": "R2_BUCKET",
          "bucket_name": "production"
        }
      ]
    }
  }
}